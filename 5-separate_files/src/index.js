// Use the code from previous task but create a separate file
// for the functional component and import it in index.js 

import React from "react"
import ReactDOM from "react-dom"
import MyInfo from "./components/MyInfo"

ReactDOM.render(
    <MyInfo />,
    document.getElementById("root")
)